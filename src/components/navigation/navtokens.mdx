import {Meta} from '@storybook/addon-docs/blocks';
import {ColorTokenList} from '../../docs/doccomponents/colortokenlist/colortokenlist.tsx';
import {lightTokens} from '@visma-severa/severa-design-tokens';

<Meta title="Components/Navigation/Tokens" />

<ColorTokenList
    title="Navigation tokens"
    tokens={Object.fromEntries(Object.entries(lightTokens.color).filter(([key]) => key.startsWith('nav')))}
/>
