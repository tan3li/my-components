import {ColorTokenList} from '../../docs/doccomponents/colortokenlist/colortokenlist.tsx';
import {Meta} from '@storybook/addon-docs/blocks';
import {lightTokens} from '@tan3li/d-tokens';

<Meta title="Components/Data Visualization/Chart tokens" />

<ColorTokenList
    title="Chart tokens"
    tokens={Object.fromEntries(Object.entries(lightTokens.color).filter(([key]) => key.startsWith('chart')))}
/>
