import {ColorTokenList} from '../../docs/doccomponents/colortokenlist/colortokenlist.tsx';
import {Meta} from '@storybook/addon-docs/blocks';
import {lightTokens} from '@visma-severa/severa-design-tokens';

<Meta title="Components/Feedback/Tokens" />

<ColorTokenList
    title="Feedback tokens"
    tokens={Object.fromEntries(Object.entries(lightTokens.color).filter(([key]) => key.startsWith('feedback')))}
/>
