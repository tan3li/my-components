import {Meta} from '@storybook/addon-docs/blocks';
import {ColorTokenList} from '../../docs/doccomponents/colortokenlist/colortokenlist.tsx';
import {lightTokens} from '@tan3li/d-tokens';

<Meta title="Components/Inputs/Tokens" />

<ColorTokenList
    title="Input tokens"
    tokens={Object.fromEntries(Object.entries(lightTokens.color).filter(([key]) => key.startsWith('input')))}
/>
