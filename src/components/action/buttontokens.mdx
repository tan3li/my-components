import {Meta} from '@storybook/addon-docs/blocks';
import {ColorTokenList} from '../../docs/doccomponents/colortokenlist/colortokenlist.tsx';
import {lightTokens} from '@visma-severa/severa-design-tokens';

<Meta title="Components/Action/Button tokens" />

<ColorTokenList
    title="Button tokens"
    tokens={Object.fromEntries(Object.entries(lightTokens.color).filter(([key]) => key.includes('Button')))}
/>
